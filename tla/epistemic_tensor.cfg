SPECIFICATION Spec

CONSTANTS
    GroundTruths = {paris, wombat}
    PlausibleLies = {glavinsky}
    ObviousLies = {westphalia}
    Refusals = {no_such_treaty}

\* Expected to HOLD: the tensor judge never accepts something the
\* text judge would reject.
INVARIANT TensorNoWorseThanText

\* Expected to HOLD: everything that defeats the tensor judge is a
\* fluent fabrication.
INVARIANT ResidueIsPlausibleLies

\* Expected to HOLD: low entropy implies neither grounded nor fabricated.
INVARIANT EntropyIsNotAVerdict

\* Expected to be VIOLATED. Checked separately; see run_tlc.sh.
\* INVARIANT Verifiability
