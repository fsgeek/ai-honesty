SPECIFICATION Spec

\* Same model values as the impossibility spec
CONSTANTS
    GroundTruths = {paris, wombat}
    PlausibleLies = {glavinsky}
    ObviousLies = {westphalia}

\* This invariant should HOLD (unlike the impossibility spec)
INVARIANT Verifiability
